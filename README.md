# Spools Index

This repository is the Suture spools registry. Each spool entry lives under
`entries/<name>/<version>/spool.toml`.

Use the `Spool Upload` workflow to add new entries, or trigger it via
`repository_dispatch` from the CLI/API when ready.
